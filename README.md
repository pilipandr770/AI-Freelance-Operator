# AI Freelance Operator

## Опис проєкту

**AI Freelance Operator** — це персональна мультиагентна система автоматизації фриланс-операцій, яка:

- автоматично читає email-повідомлення з платформ фрилансу (Freelancer.com)
- аналізує отримані проєкти
- веде переговори із замовниками
- формує технічні завдання
- оцінює вартість і час виконання
- надсилає комерційні пропозиції та інвойси
- відстежує оплату
- створює pipeline готових до виконання проєктів
- інформує власника через Telegram
- надає веб-адмінку для управління всією системою

Система створена для персонального використання та побудована як **AI workflow automation platform**.

## Ціль системи

Автоматизувати повний цикл роботи з клієнтами:

```
Inbound email → аналіз → переговори → ТЗ → оцінка → інвойс → оплата → проект
```

та дозволити власнику:

- отримувати тільки готові до виконання проєкти
- керувати політиками роботи через адмінку
- контролювати всі процеси через Telegram

## Основні функції

### Intake Pipeline

Система:

1. Підключається до Gmail через IMAP
2. Отримує нові повідомлення
3. Розбиває email на окремі задачі
4. Фільтрує:
   - scam
   - illegal
5. Класифікує задачі:
   - MICRO
   - SMALL
   - MEDIUM
   - LARGE
   - RND
6. Визначає:
   - знайомий стек / новий стек
   - складність
   - достатність інформації

### Negotiation Pipeline

Система автоматично:

- формує перший лист клієнту
- веде переписку
- збирає технічні вимоги
- формує структуроване ТЗ
- оцінює кількість годин
- розраховує бюджет
- надсилає офер
- надсилає інвойс (50% prepayment)
- відстежує підтвердження

### Project State Machine

Кожен проєкт має статус:

- `NEW`
- `ANALYZED`
- `NEGOTIATION`
- `REQUIREMENTS_COLLECTION`
- `ESTIMATION_READY`
- `OFFER_SENT`
- `AGREED`
- `FUNDED`
- `EXECUTION_READY`
- `CLOSED`

### Admin Control Panel

Веб-адмінка (доступна за адресою `/admin`) надає повний контроль над системою:

**Dashboard:**
- статистика проєктів та клієнтів
- останні активності
- статус системи (база даних, OpenAI API)

**AI Agents Management:**
- перегляд всіх AI агентів
- редагування інструкцій та промптів для кожного агента
- додавання нових агентів
- активація/деактивація агентів
- версіонування інструкцій

**System Settings:**
- погодинна ставка
- політики переговорів
- пороги для фільтрації scam
- мінімальний/максимальний бюджет проєктів

**Projects & Clients:**
- перегляд всіх проєктів
- картки клієнтів
- зміна статусів вручну

**Activity Logs:**
- логи всіх дій AI агентів
- використані токени та вартість
- час виконання запитів

### Telegram Owner Assistant

Telegram-бот надсилає:

- нові проєкти
- відправлені офери
- отримані оплати
- проблемні переговори
- статуси виконання

та підтримує команди:

- `/projects`
- `/project <id>`
- `/settings`
- `/pause_auto`
- `/resume_auto`

## Архітектура системи

### Core Layers

#### 1. Email Intake Layer

- Gmail IMAP connector
- Email ingestion worker
- Message storage

#### 2. AI Agent Layer

Агенти:

- Email Parser
- Task Splitter
- Scam Filter
- Classification Agent
- Requirement Engineer
- Estimation Agent
- Pricing Engine
- Offer Generator
- Agreement Detector
- Dialogue Orchestrator

#### 3. Workflow Engine

Stage-driven orchestration:

```python
if stage == ANALYZED → start negotiation
if stage == REQUIREMENTS_READY → estimate
if stage == AGREED → invoice
```

#### 4. Data Layer

PostgreSQL:

- `clients`
- `projects`
- `tasks`
- `project_states`
- `agent_logs`
- `system_settings`
- `agent_instructions`

#### 5. Owner Control Layer

- Admin panel
- Telegram bot
- Runtime settings

## Основні принципи системи

### State-Driven Architecture

Агенти не взаємодіють між собою напряму.
Вони працюють через Project State у БД.

### Single Negotiator Principle

З клієнтом спілкується лише Dialogue Orchestrator.
Інші агенти виконують вузькі задачі.

### Deterministic Pricing

LLM оцінює години, але ціна розраховується кодом:

```python
price = hours × hourly_rate
```

### Runtime Configurable System

Політики змінюються через адмінку без деплою.

## Технології

- Python
- Flask
- PostgreSQL
- OpenAI API
- IMAP (Gmail)
- Telegram Bot API

## Поточний етап розробки

Версія v1 включає:

- ✅ Flask skeleton
- ✅ Database schema (PostgreSQL)
- ✅ OpenAI API integration
- ✅ Admin control panel with agent management
- ✅ System settings management
- ✅ Agent logs tracking
- 🔄 Email ingestion (в розробці)
- 🔄 Intake pipeline (в розробці)
- 🔄 Negotiation pipeline (в розробці)
- 🔄 Telegram owner bot (в розробці)

**Execution outsourcing pipeline** буде додано у наступних версіях.

## Використання

### Швидкий старт

1. **Встановлення залежностей:**
```bash
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

2. **Налаштування .env:**
```bash
copy .env.example .env
# Відредагуйте .env та додайте свої ключі
```

3. **Ініціалізація бази даних:**
```bash
# Створіть БД в PostgreSQL
createdb ai_freelance_operator

# Ініціалізуйте схему через API або psql
python run.py
# Потім: POST http://localhost:5000/api/db/init
```

4. **Запуск сервера:**
```bash
python run.py
# або
start.bat
```

5. **Відкрийте адмін панель:**
```
http://localhost:5000/admin
```

### Керування AI Агентами

**Додавання нового агента:**
1. Відкрийте `/admin/agents`
2. Натисніть "+ Add New Agent"
3. Вкажіть:
   - **Agent Name** - унікальне ім'я (наприклад, `email_parser`)
   - **System Prompt** - основна роль агента
   - **Instruction Text** - детальні інструкції
4. Збережіть

**Редагування інструкцій:**
1. Знайдіть агента у списку
2. Натисніть "Edit"
3. Змініть промпти або інструкції
4. Збережіть (версія автоматично збільшиться)

**Деактивація агента:**
- Натисніть "Deactivate" щоб тимчасово відключити агента
- Агент не буде використовуватись системою

### Налаштування системи

**Через Admin Panel (`/admin/settings`):**
- Змінюйте погодинну ставку
- Налаштовуйте пороги для scam фільтрації
- Встановлюйте мінімальні/максимальні бюджети

**Через API:**
```bash
curl -X POST http://localhost:5000/api/settings \
  -H "Content-Type: application/json" \
  -d '{"key": "hourly_rate", "value": "75.0", "value_type": "float"}'
```

## Довгострокова мета

Створення персональної автоматизованої AI-операційної системи, яка:

- самостійно генерує pipeline проектів
- автоматично веде переговори
- автоматично створює оплачувані задачі
- мінімізує ручну участь власника

## Автор

Персональна система автоматизації фриланс-операцій, створена для індивідуального використання.

## Далі

Після цього README наступним кроком у проєкті буде:

1. створення DB schema
2. реалізація intake worker
3. створення першого AI-agent pipeline